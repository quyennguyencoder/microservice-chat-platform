package com.nguyenquyen.conversationservice.entity;

public enum ChatType {

    /** One-on-one private chat between exactly two users. */
    PRIVATE,

    /** Group chat with multiple participants (future feature). */
    GROUP
}
